apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: mahesh-cluster-role
rules:
- apiGroups: ["","apps"]
  resources: ["nodes", "namespaces", "pods", "services", "deployments", "replicasets", "configmaps", "secrets", "statefulsets", "daemonsets"]
  verbs: ["get", "list", "watch", "create", "delete", "update", "patch"]